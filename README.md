# LLM API Tests

Test scripts for:
- Qwen-Max (text)
- Wen-TTS (speech)
- Wan2.5 (video)

## Setup
```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env  # and fill your API key
```
